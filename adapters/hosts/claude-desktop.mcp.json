{
  "mcpServers": {
    "galois": {
      "command": "{{GALOIS_PYTHON}}",
      "args": [
        "-m",
        "global_memory",
        "mcp",
        "stdio"
      ],
      "env": {
        "PYTHONPATH": "{{GALOIS_ROOT}}\\src",
        "GALOIS_ROOT": "{{GALOIS_ROOT}}"
      }
    }
  }
}
